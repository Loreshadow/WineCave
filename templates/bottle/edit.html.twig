{% extends 'base.html.twig' %}

{% block title %}Modifier la bouteille{% endblock %}

{% block body %}
<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <h4><i class="fas fa-edit"></i> Modifier la bouteille</h4>
        </div>
        <div class="auth-body">
            {{ form_start(form, {'attr': {'class': 'auth-form', 'enctype': 'multipart/form-data'}}) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary btn-full">
                    <i class="fas fa-save"></i> Enregistrer les modifications
                </button>
            {{ form_end(form) }}
            <div class="auth-footer">
                <p><a href="{{ path('cave_index') }}"><i class="fas fa-arrow-left"></i> Retour à ma cave</a></p>
            </div>
        </div>
    </div>
</div>
{% endblock %}